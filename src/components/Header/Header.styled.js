import styled from "styled-components";

export const Main = styled.h1(() => ({
  alignItems: "center",
  textAlign: "center",
}));

export const Cart = styled.div(() => ({
  padding: "1rem",
  display: "flex",
}));
